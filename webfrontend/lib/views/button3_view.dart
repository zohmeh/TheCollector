import 'package:flutter/material.dart';

class Button3View extends StatelessWidget {
  static const routeName = '/button3view';
  //const Button3View({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Button3View'),
    );
  }
}
