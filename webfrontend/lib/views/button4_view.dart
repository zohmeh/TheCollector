import 'package:flutter/material.dart';

class Button4View extends StatelessWidget {
  const Button4View({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Button4View'),
    );
  }
}
